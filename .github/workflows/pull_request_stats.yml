on:
  pull_request:
    types: [opened, synchronize]
  push:
    branches:
      - canary

name: Generate Stats

concurrency:
  # Keep cancel-on-update behavior for PRs, but allow canary pushes to run independently.
  group: ${{ github.workflow }}-${{ github.event_name == 'pull_request' && github.ref || github.run_id }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

env:
  NAPI_CLI_VERSION: 2.18.4
  TURBO_VERSION: 2.9.4
  TEST_CONCURRENCY: 6

  TURBO_TEAM: 'vtest314-next-adapter-e2e-tests'
  # Prefer shared remote cache across runs, but keep local cache enabled so jobs
  # degrade gracefully if the remote cache or token is unavailable.
  TURBO_CACHE: 'local:rw,remote:rw'
  TURBO_TOKEN: ${{ secrets.TURBO_TOKEN }}
  NEXT_TELEMETRY_DISABLED: 1
  # Vercel KV Store for test timings
  KV_REST_API_URL: ${{ secrets.KV_REST_API_URL }}
  KV_REST_API_TOKEN: ${{ secrets.KV_REST_API_TOKEN }}
  NEXT_TEST_JOB: 1
  NEXT_DISABLE_SWC_WASM: 1

jobs:
  pr-ci-metadata:
    name: Upload PR CI metadata
    if: ${{ github.event_name == 'pull_request' }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Write PR metadata
        env:
          PR_NUMBER: ${{ github.event.pull_request.number }}
          PR_HEAD_SHA: ${{ github.event.pull_request.head.sha }}
          PR_HEAD_REF: ${{ github.event.pull_request.head.ref }}
          PR_HEAD_REPO: ${{ github.event.pull_request.head.repo.full_name }}
          PR_BASE_REF: ${{ github.event.pull_request.base.ref }}
          PR_IS_FORK: ${{ github.event.pull_request.head.repo.full_name != github.repository }}
        run: |
          mkdir -p pr-ci-metadata
          node <<'NODE'
          const fs = require('fs')

          fs.writeFileSync(
            'pr-ci-metadata/pr.json',
            JSON.stringify(
              {
                number: Number(process.env.PR_NUMBER),
                headSha: process.env.PR_HEAD_SHA,
                headRef: process.env.PR_HEAD_REF,
                headRepo: process.env.PR_HEAD_REPO,
                baseRef: process.env.PR_BASE_REF,
                isFork: process.env.PR_IS_FORK === 'true',
              },
              null,
              2
            )
          )
          NODE

      - name: Upload PR metadata
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: pr-ci-metadata
          path: pr-ci-metadata/pr.json
          retention-days: 1

  build:
    uses: ./.github/workflows/build_reusable.yml
    secrets: inherit
    with:
      stepName: 'generate-pull-request-stats'
      uploadSwcArtifact: 'yes'
      uploadAnalyzerArtifacts: 'yes'

  stats:
    name: Stats (${{ matrix.bundler }})
    needs: build
    # The webpack benchmark alone takes ~25 minutes, so anything at or below
    # that cancels the job right as it finishes (seen on canary and PRs alike).
    timeout-minutes: 35
    strategy:
      fail-fast: false
      matrix:
        # Keep in sync with STATS_EXPECTED_BUNDLERS
        bundler: [webpack, turbopack]
    runs-on: ubuntu-latest-16-core-arm-oss
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          fetch-depth: 25
          persist-credentials: false

      - name: Check non-docs only change
        run: echo "DOCS_CHANGE<<EOF" >> $GITHUB_OUTPUT; echo "$(node scripts/run-for-change.mjs --not --type docs --exec echo 'nope')" >> $GITHUB_OUTPUT; echo 'EOF' >> $GITHUB_OUTPUT
        id: docs-change

      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        if: ${{ steps.docs-change.outputs.DOCS_CHANGE == 'nope' }}
        with:
          name: next-swc-binary-${{ runner.os }}-${{ runner.arch }}
          path: packages/next-swc/native

      - run: cp -r packages/next-swc/native .github/actions/next-stats-action/native
        if: ${{ steps.docs-change.outputs.DOCS_CHANGE == 'nope' }}

      - uses: ./.github/actions/next-stats-action
        if: ${{ steps.docs-change.outputs.DOCS_CHANGE == 'nope' }}
        with:
          bundler: ${{ matrix.bundler }}
        env:
          TURBO_TEAM: ${{ env.TURBO_TEAM }}
          TURBO_TOKEN: ${{ secrets.TURBO_TOKEN }}
          TURBO_CACHE: ${{ env.TURBO_CACHE }}
          PREVIEW_BUILDS_BASE_URL: ${{ vars.PREVIEW_BUILDS_BASE_URL }}

      - name: Upload stats results
        if: ${{ steps.docs-change.outputs.DOCS_CHANGE == 'nope' }}
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: pr-stats-${{ matrix.bundler }}
          path: pr-stats-${{ matrix.bundler }}.json
          retention-days: 1

  stats-aggregate:
    name: Aggregate Stats
    needs: stats
    # Always run so we can recover partial results. A single bundler timing out
    # cancels its job, which makes `cancelled()` true here and marks the whole
    # run "cancelled" even though the other bundler finished, so `!cancelled()`
    # would wrongly skip aggregation. Running unconditionally lets us aggregate
    # whatever bundlers succeeded; the reason a bundler is missing is reported in
    # the comment, and a truly cancelled run (no results at all) is handled by
    # the cancelled fallback in scripts/pr-ci-comment.mjs.
    if: ${{ always() }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          fetch-depth: 25
          persist-credentials: false

      - name: Check non-docs only change
        run: echo "DOCS_CHANGE<<EOF" >> $GITHUB_OUTPUT; echo "$(node scripts/run-for-change.mjs --not --type docs --exec echo 'nope')" >> $GITHUB_OUTPUT; echo 'EOF' >> $GITHUB_OUTPUT
        id: docs-change

      - name: Download all stats artifacts
        if: ${{ steps.docs-change.outputs.DOCS_CHANGE == 'nope' }}
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          pattern: pr-stats-*
          path: stats-results
          merge-multiple: true

      - name: Setup Node.js
        if: ${{ steps.docs-change.outputs.DOCS_CHANGE == 'nope' }}
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        with:
          node-version-file: .node-version
          package-manager-cache: false

      - name: Install dependencies
        if: ${{ steps.docs-change.outputs.DOCS_CHANGE == 'nope' }}
        working-directory: .github/actions/next-stats-action
        run: npm install

      - name: Aggregate and post results
        if: ${{ steps.docs-change.outputs.DOCS_CHANGE == 'nope' }}
        working-directory: .github/actions/next-stats-action
        run: node src/aggregate-results.js ${{ github.workspace }}/stats-results
        env:
          KV_REST_API_URL: ${{ secrets.KV_REST_API_URL }}
          KV_REST_API_TOKEN: ${{ secrets.KV_REST_API_TOKEN }}
          # Keep in sync with the `stats` job matrix above. The aggregate uses it
          # to detect and report bundlers whose job failed or was cancelled
          # (their pr-stats-<bundler>.json artifact will be missing).
          STATS_EXPECTED_BUNDLERS: 'webpack turbopack'
          # Collapsed matrix result ('success' | 'failure' | 'cancelled' |
          # 'skipped'). Used to decide, when no artifacts exist at all, whether
          # the bundlers genuinely failed (post an "all failed" notice) or the
          # run was cancelled/superseded (stay silent and let pr-ci-comment.mjs
          # post the cancelled notice).
          STATS_RESULT: ${{ needs.stats.result }}I'll help you find a solution for this failing job. Let me start by retrieving the job logs.I'll retrieve the job logs to identify the failure and provide a solution.Now let me get the relevant files to understand the context:Now let me examine the workflow file and the aggregation script to understand the issue:## Problem

The job is failing because the `stats-aggregate` step tries to read from the `stats-results` directory, but it doesn't exist. The error occurs at line 55 in `aggregate-## Problem Analysis

The job is failing because the `stats-aggregate` step in the workflow is trying to read from a `stats-results` directory that doesn't exist. The error occurs at line 55 in `aggregate-results.js`:

```
Error: ENOENT: no such file or directory, scandir '/home/runner/work/next.js/next.js/stats-results'
```

This happens because the upstream `stats` job failed for both bundlers (webpack and turbopack), so no artifacts were produced to download.

## Root Cause

Looking at the workflowresults.js`:

```
Error: ENOENT: no such file or directory, scandir '/home/runner/work/next.js/next.js/stats-results'
```

This happens because:

1. The `stats` job (which runs the webpack/turbopack benchmarks) is **failing or not producing artifacts**
2. When artifacts logs, `STATS_RESULT: failure` indicates the stats jobs failed. When the `stats` job fails:

1. No `pr-stats-*.json` artifacts are created
2. The `Download all stats artifacts` step (line 156-162 in the workflow) downloads with `pattern: pr-stats-*` into fail to download in the `Download all stats artifacts` step (line 156-162 in the workflow), the `stats-results` directory is never created
3. The `Aggregate and post results` step then attempts to read a non-existent directory, causing the script to crash

## Root Cause

Looking at the environment variables in the logs:
- `STATS the `stats-results` directory
3. When no artifacts match the pattern, the `stats-results` directory is never created
4. The aggregate script assumes the directory exists and tries to `scandir` it, causing the crash

## Solution

The `aggregate-results.js` script needs to handle the case where the `stats-results` directory doesn't exist. Add a directory existence check before attempting to read it:

**File:** [`.github/actions_EXPECTED_BUNDLERS: webpack turbopack` (expects both bundlers)
- `STATS_RESULT: failure` (indicates stats jobs failed)

Since both bundler jobs failed, no `pr-stats-*.json` artifacts were created to download.

## Solution

The `aggregate-results.js` script needs to handle the case where the `stats-results` directory doesn't/next-stats-action/src/aggregate-results.js`](https://github.com/vercel/next.js/blob/0491db047b8f9c4a5f9d0285ad9ed514bb134873/.github/actions/next-stats-action/src/aggregate-results. exist. Modify the script to check if the directory exists before attempting to read it:

**File:** `.github/actions/next-stats-action/src/aggregate-results.js`

```javascript
async function main() {
  const resultsDir = process.argv[2] || process.cwd()

  logger(`Aggregating results from: ${resultsDir}`)

  // Check if thejs)

**Change (lines 49-58):**

```javascript
async function main() {
  const resultsDir = process.argv[2] || process.cwd()

  logger(`Aggregating results from: ${resultsDir}`)

  // Check if results directory exists; if not, treat as no artifacts
  if (!existsSync(resultsDir)) {
    logger(`Results directory does not exist: ${resultsDir}`)
    const statsResult results directory exists
  if (!existsSync(resultsDir)) {
    logger(`Results directory does not exist: ${resultsDir}`)
    
    const statsResult = process.env.STATS_RESULT
    const expectedBundlers = getExpectedBundlers()
    
    if (expectedBundlers.length > 0 && statsResult === 'failure') {
      logger(
        `No pr-stats-*.json files found - all bundler jobs ` +
          `(${expectedBundlers.join(', ')}) failed`
      )
      await = process.env.STATS_RESULT
    const expectedBundlers = getExpectedBundlers()
    
    if (expectedBundlers.length === 0 || statsResult !== 'failure') {
      logger(
        `No results directory (stats result: ${statsResult || 'unknown'}) - nothing to aggregate`
      )
      process.exit(0)
    }

    logger(
      `No results directory - all bun addComment(
        [],
        { isRelease: false, commitId: null },
        {},
        { missingBundlers: expectedBundlers.map(getBundlerDisplayName) }
      )
    }
    process.exit(0)
  }

  // Find all pr-stats-*.json files
  const files = await fs.readdir(resultsDir)
  // ... rest of the function
```

**Key changes:**
- Add `existsSync(dler jobs ` +
        `(${expectedBundlers.join(', ')}) failed`
    )

    await addComment(
      [],
      { isRelease: false, commitId: null },
      {},
      { missingBundlers: expectedBundlers.map(getBundlerDisplayName) }
    )
    process.exit(0)
  }

  // Find all pr-stats-*.json files
  const files = await fs.readdir(resultsDir)
  // ... rest of the function
```resultsDir)` check before calling `fs.readdir()`
- When the directory doesn't exist, handle it gracefully by checking the `STATS_RESULT` environment variable
- If stats jobs genuinely failed, post the "all bundlers failed" notice
- Exit cleanly with code 0 instead of crashing

This allows the workflow to complete without error and properly report which bundlers failed, rather than crashing with an `ENOENT` error.
